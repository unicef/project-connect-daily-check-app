import { FormatThroughputDisplayPipe } from './format-throughput-display.pipe';

describe('FormatThroughputDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatThroughputDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
