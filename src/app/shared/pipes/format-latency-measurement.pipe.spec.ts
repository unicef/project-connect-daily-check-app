import { FormatLatencyMeasurementPipe } from './format-latency-measurement.pipe';

describe('FormatLatencyMeasurementPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatLatencyMeasurementPipe();
    expect(pipe).toBeTruthy();
  });
});
