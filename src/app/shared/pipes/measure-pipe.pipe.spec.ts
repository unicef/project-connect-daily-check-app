import { MeasurePipePipe } from './measure-pipe.pipe';

describe('MeasurePipePipe', () => {
  it('create an instance', () => {
    const pipe = new MeasurePipePipe();
    expect(pipe).toBeTruthy();
  });
});
