import { FormatProbabilityMeasurementPipe } from './format-probability-measurement.pipe';

describe('FormatProbabilityMeasurementPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatProbabilityMeasurementPipe();
    expect(pipe).toBeTruthy();
  });
});
