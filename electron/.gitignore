# NPM renames .gitignore to .npmignore
# In order to prevent that, we remove the initial "."
# And the CLI then renames it
app
node_modules
build
dist
logs
out
*.env

# Generated before each Electron build by scripts/generate-build-mode.js
src/build-mode.ts